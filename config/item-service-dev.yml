spring:
  application:
    name: item-service
    
server:
  port: 8001

#注册eureka
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka,http://eureka2:2002/eureka
      
      
#禁止配置中心的配置将客户端配置覆盖掉    
